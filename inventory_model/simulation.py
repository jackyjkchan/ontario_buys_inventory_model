
def run_sim(config):
    assert ("demand_mean" in config)
    assert ("order_lead_time" in config)
    assert ("demand_mean" in config)
    assert ("demand_mean" in config)
    assert ("demand_mean" in config)


